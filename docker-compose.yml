version: '3.3'
services:
  mongodb:
    image: mongo
    env_file:
      - .env
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - mongodb:/data/db
    ports:
      - $DB_PORT:$DB_PORT
    restart: unless-stopped

volumes:
  my-db:
  mongodb: