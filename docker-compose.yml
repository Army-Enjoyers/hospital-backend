version: '3.3'
services:
  mongodb:
    image: mongo
    env_file:
      - .env
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - mongodb:/data/db
    restart: unless-stopped

  backend:
    build: .
    env_file:
      - .env
    depends_on:
      - mongodb
    links:
      - mongodb
    ports:
      - 8080:8080

volumes:
  my-db:
  mongodb: